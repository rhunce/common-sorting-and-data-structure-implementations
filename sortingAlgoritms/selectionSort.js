// numbers = [5, 3, 1, 2, 4]

// O(n^2) time | O(n) space
// iterate over numbers from i = 0 to end - 1
    // iterate over numbers from j = i to end - 1
        // find min
        // swap element at location of min with element at i
// return numbers
function selectionSort(numbers) {
    // 
}